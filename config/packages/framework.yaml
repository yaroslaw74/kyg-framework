framework:
    secret: "%env(APP_SECRET)%"
    form:
        enabled: true
    http_method_override: true
    session:
        handler_id: ~
    serializer:
        mapping:
            paths: ["%kernel.project_dir%/config/serialization"]
