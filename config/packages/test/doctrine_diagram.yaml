# https://github.com/jawira/doctrine-diagram-bundle
doctrine_diagram:
    er:
        filename: "%kernel.project_dir%/tests/er"
        size: midi
    class:
        filename: "%kernel.project_dir%/tests/class"
        size: midi
    convert:
        format: svg
        converter: auto
